[
{ type: install
  message: <<EOM
If your desktop environment such GNOME, Cinnamon, MATE, or Pantheon needs
to unlock the gnome-keyring daemon. Add following lines in each
LightDM PAM configuration files, %%PREFIX%%/etc/pam.d/

  # auth
  [...]
  auth	optional	%%PREFIX%%/lib/pam_gnome_keyring.so

  # account
  [...]

  # session
  [...]
  session	optional	%%PREFIX%%/lib/pam_gnome_keyring.so	auto_start

  # password
  [...]
EOM
}
]
