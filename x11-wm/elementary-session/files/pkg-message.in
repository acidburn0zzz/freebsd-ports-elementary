[
{ type: install
  message: <<EOM
To have panel automatically launched:
  mkdir -p ~/.config/autostart
  cp %%EXAMPLESDIR%%/plank.desktop ~/.config/autostart/

Then copy 'xprofile' into your home directory:
  cp %%EXAMPLESDIR%%/xprofile ~/.xprofile

If you want to launch session from a console (without login manager)
  cp %%EXAMPLESDIR%%/xinitrc ~/.xinitrc
EOM
}
]
