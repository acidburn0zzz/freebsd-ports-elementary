--- meson.build.orig	2021-12-13 22:46:29 UTC
+++ meson.build
@@ -4,17 +4,10 @@ project(
 )
 
 i18n = import('i18n')
-symlink = meson.current_source_dir() / 'meson' / 'symlink.py'
 
 install_subdir(
     'backgrounds',
     install_dir: get_option('datadir')
-)
-
-meson.add_install_script(
-    symlink,
-    '--src', get_option('datadir') / 'backgrounds' / 'odin-dark.jpg',
-    '--dest', get_option('datadir') / 'backgrounds' / 'elementaryos-default'
 )
 
 subdir('data')
